Execute these commands to create a pyhton virtual environment, activate it and install the package dependencies.
´´´bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
´´´
